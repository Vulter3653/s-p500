name: Find 2007 continuation run

on:
  pull_request:
    paths:
      - '.github/workflows/find-2007-continuation.yml'

permissions:
  actions: read
  contents: read

jobs:
  lookup:
    runs-on: ubuntu-24.04
    steps:
      - name: Query runs for 2008 publication merge SHA
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          python - <<'PY'
          import json
          import os
          import urllib.request

          sha = "3de2001fad495e512ceb0d0d91ee9e0d4788a810"
          url = (
              "https://api.github.com/repos/Vulter3653/s-p500/actions/runs"
              f"?head_sha={sha}&per_page=30"
          )
          request = urllib.request.Request(
              url,
              headers={
                  "Accept": "application/vnd.github+json",
                  "Authorization": f"Bearer {os.environ['GH_TOKEN']}",
                  "X-GitHub-Api-Version": "2022-11-28",
                  "User-Agent": "s-p500-run-diagnostic",
              },
          )
          with urllib.request.urlopen(request, timeout=60) as response:
              payload = json.load(response)
          selected = [
              {
                  key: run.get(key)
                  for key in (
                      "id", "name", "event", "status", "conclusion",
                      "head_sha", "head_branch", "created_at", "updated_at", "html_url",
                  )
              }
              for run in payload.get("workflow_runs", [])
          ]
          print(json.dumps(selected, indent=2, sort_keys=True))
          PY
